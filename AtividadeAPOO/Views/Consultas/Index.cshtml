@model IEnumerable<Modelo.Cadastros.Consulta>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Adicionar Consulta", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Data_hora)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sintomas)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Data_hora)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sintomas)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id=item.ConsultaId }) |
                @Html.ActionLink("Detalhes", "Details", new { id=item.ConsultaId }) |
                @Html.ActionLink("Deletar Consulta", "Delete", new { id=item.ConsultaId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
